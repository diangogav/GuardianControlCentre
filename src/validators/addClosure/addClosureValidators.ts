import { FormControl } from "@angular/forms";

export class AddClosureValidators{

  static checkDateActualStartClosure(control: String) {

    console.log(control);
    
    /*const value: string = control.value;

    var actualDate = new Date();

        var values;
        var date;
        var month;
        var year;

          

        values = value.split("-");
        date = values[2];
        month = values[1];
        year = values[0];

        var actualStartClosureDate = new Date(
            year,
            (month-1),
            date

        )*/

        
      

  }

}
